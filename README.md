# task_management_app

## 📝 Опис проєкту

**Task Management App** — це багатокомпонентний веб-додаток для управління завданнями в команді.
Користувачі можуть створювати, редагувати, видаляти завдання, призначати їх іншим користувачам та відстежувати статус виконання.

Проєкт побудований на архітектурі мікросервісів і містить повноцінне середовище спостереження та логування (Prometheus, Grafana, ELK Stack).  
Розгортання виконується через Docker Compose, що забезпечує ізольоване середовище для розробки та продакшн-деплойменту.

## ⚙️ Основні компоненти
| Компонент                                                 | Технологія            | Призначення                                                         |
| --------------------------------------------------------- | --------------------- | ------------------------------------------------------------------- |
| **Frontend**                                              |        ReactJS        | Відповідає за відображення списку завдань і їхніх статусів.  Взаємодія (створення, оновлення, видалення) відбувається через API, що дозволяє тестувати запити за допомогою Postman або інших інструментів. |
| **Backend**                                               |    Python (Flask)     | REST API для обробки запитів, взаємодії з БД та логіки авторизації. |
| **Database**                                              |      PostgreSQL       | Зберігання даних про користувачів, завдання, статуси.               |
| **Nginx**                                                 |     Reverse Proxy     | Маршрутизація запитів між фронтендом і бекендом, підтримка HTTPS.   |
| **Prometheus, Grafana, cAdvisor**                         |      Моніторинг       | Збір метрик і візуалізація продуктивності додатку.                  |
| **ELK Stack (Elasticsearch, Logstash, Filebeat, Kibana)** |       Логування       | Централізований збір, обробка та перегляд логів.                    |
| **Docker Compose**                                        |      Оркестрація      | Керує запуском усіх контейнерів та їх взаємодією.                   |

## 🐳 Docker-образи
| Сервіс               | Образ                                                 | Примітка                                           |
| -------------------- | ----------------------------------------------------- | -------------------------------------------------- |
| **Backend**          | `python:3.12-slim`                                    | Базовий образ для Flask API                        |
| **Database**         | `postgres:16-alpine`                                  | Легка версія PostgreSQL                            |
| **Frontend**         | `node:18-alpine`                                      | Збірка React-додатку                               |
| **frontend-metrics** | `node:18-alpine`                                      | Збір та експорт базових системних метрик фронтенду |
| **Nginx**            | `nginx:alpine`                                        | Реверс-проксі для frontend/backend                 |
| **Prometheus**       | `prom/prometheus:v3.6.0`                              | Моніторинг                                         |
| **Grafana**          | `grafana/grafana:12.2`                                | Візуалізація метрик                                |
| **cAdvisor**         | `gcr.io/cadvisor/cadvisor:v0.52.1`                    | Моніторинг контейнерів                             |
| **Elasticsearch**    | `docker.elastic.co/elasticsearch/elasticsearch:9.1.5` | Сховище логів                                      |
| **Kibana**           | `docker.elastic.co/kibana/kibana:9.1.5`               | Інтерфейс для логів                                |
| **Filebeat**         | `docker.elastic.co/beats/filebeat:9.1.5`              | Збір логів                                         |
| **Logstash**         | `docker.elastic.co/logstash/logstash:9.1.5`           | Обробка логів                                      |

## 🔧 Початкова ініціалізація:
1) Для того щоб Docker Engine обробляв команди Docker, повинен бути встановлений Docker Desktop.
2) Клонуйте репозиторій:  
   ```plaintext 
   git clone https://github.com/VasylHomonai/task_management_app.git  
   cd task_management_app
   ```
3) У директорії backend є файл-шаблон .env.sample На його основі, в даній директорії, потрібно створити файлв .env і задати власний секретний ключ для JWT (використовується для підпису токенів авторизації).
4) На основі файлів db_password.txt.sample та secrets/limited_user_password.txt.sample створити тут же файли db_password.txt та limited_user_password.txt і задати в них відповілні паролі до БД PostgreSQL.
   - db_password.txt — пароль для користувача postgres (суперкористувач бази даних).
   - limited_user_password.txt — пароль для обмеженого користувача limited_user, який має лише необхідні права доступу до бази app_db (чтення, запис, оновлення, видалення).
5) SSL сертифікат (self-signed). Для локального середовища SSL-сертифікат створюється автоматично за допомогою скрипта entrypoint.sh, який виконується при запуску контейнера Nginx.
   - Сертифікат самопідписаний (self-signed), тобто не виданий офіційним центром сертифікації (CA).
   - Використовується тільки для локальної розробки `https://localhost`
   - Попередження у браузері. При відкритті сторінки HTTPS браузер покаже повідомлення: Ваше з’єднання не є конфіденційним (Your connection is not private). Це очікувана поведінка для self-signed сертифікатів. Щоб продовжити, потрібно натиснути «Перейти на сторінку localhost (небезпечно)».
6) Під час першого запуску сервісу PostgreSQL автоматично виконується ініціалізація БД за допомогою скриптів із директорії `db-init`. У цей момент створюється тестовий користувач testuser та декілька демонстраційних завдань, щоб можна було одразу перевірити, що застосунок працює коректно.

## ▶️ Використання
Запустити усі сервіси. В корені проєкту виконати команду: 
   ```plaintext 
   docker compose -f docker-compose.yml up -d --build
   ```
Якщо все виконалося без помилок, ви побачите список запущених контейнерів, наприклад: https://prnt.sc/1AAq70RrpJUT  
Перевірте статус контейнерів:
   ```plaintext 
   docker ps
   ```
Всі 12 сервісів мають відображати статус `healthy`: https://prnt.sc/kcVi4lLCAWWa

## 🧩 Перевірка роботи
Тепер коли всі контейнери запущено, відкрийте у браузері сторінку:
   ```plaintext
   https://localhost
   ```
> ⚠️ При першому відкритті браузер скоріш за все покаже попередження “Ваше з’єднання не є конфіденційним”  — це очікувано, оскільки використовується самопідписаний сертифікат.
Натисніть “Перейти на сторінку localhost (небезпечно)”, щоб продовжити.

Сторінка повинна відкритись відразу з декількома тестовими завданнями: https://prnt.sc/fgHJHuwdVi8l

### Перевірка API через Postman
Після успішного запуску контейнерів можна перевірити роботу бекенду за допомогою Postman або можна будь-якого іншого HTTP-клієнта.  
Бекенд за замовчуванням доступний за адресою: `http://localhost:5000/api/tasks/public` https://prnt.sc/BHmLBAZjs5wV  

1️⃣ Реєстрація користувача.  
POST → `http://localhost:5000/api/users/register`  
Body (JSON): 
```plaintext
{
  "username": "user",
  "password": "test123"
}
```
В БД буде створено нового користувача: https://prnt.sc/lgP4YWSJPeuK

2️⃣ Авторизація та отримання токена.  
POST → `http://localhost:5000/api/users/login`  
Body (JSON): 
```plaintext
{
  "username": "user",
  "password": "test123"
}
```
У відповідь ви отримаєте JWT токен, який потрібно використовувати для всіх захищених запитів: https://prnt.sc/y7e9_wkcp_xP

3️⃣ Отримання даних поточного користувача.  
GET → `http://localhost:5000/api/users/me`  
Цей ендпоінт дозволяє отримати інформацію про авторизованого користувача, токен якого був переданий у запиті: https://prnt.sc/24apcMs1WUVM

4️⃣ Отримання списку всіх користувачів.  
GET → `http://localhost:5000/api/users`  
Ендпоінт повертає повний список усіх користувачів у системі. Доступ до нього мають лише авторизовані користувачі: https://prnt.sc/jHXYO_oMpcBZ

5️⃣ Отримання інформації про конкретного користувача.  
GET → `http://localhost:5000/api/users/<id>`  
Ендпоінт повертає детальну інформацію про користувача за його id. Доступ мають авторизовані користувачі: https://prnt.sc/oDeYYQikHSjM

6️⃣ Оновлення інформації про користувача.  
PUT → `http://localhost:5000/api/users/<id>`  
Ендпоінт дозволяє оновити дані користувача — наприклад, змінити логін або пароль. Доступ має авторизований користувач: https://prnt.sc/hOkzdJcBHRnr

7️⃣ Видалення користувача.
DELETE → `http://localhost:5000/api/users/<id>`  
Ендпоінт дозволяє видалити користувача з бази даних. Доступ має лише авторизований користувач: https://prnt.sc/QQ00bw5p8Ccf

8️⃣ Отримання списку усіх задач.  
GET → `http://localhost:5000/api/tasks`  
Ендпоінт повертає список усіх задач. Доступ мають лише авторизовані користувачі: https://prnt.sc/F5iafBV7r3R5

9️⃣ Отримання списку усіх задач (публічний доступ).
GET → `http://localhost:5000/api/tasks/public`  
Ендпоінт повертає список усіх задач, доступний без авторизації. Використовується для веб-версії застосунку.

🔟 Створення нової задачі.  
POST → `http://localhost:5000/api/tasks`  
Ендпоінт дозволяє авторизованому користувачу створити нову задачу: https://prnt.sc/PbPkSW26WiCa

1️⃣1️⃣ Отримання інформації про конкретну задачу.  
GET → `http://localhost:5000/api/tasks/<id>`  
Ендпоінт повертає детальну інформацію про задачу за її унікальним ідентифікатором id. Доступ мають лише авторизовані користувачі. https://prnt.sc/ubw67XSkcBIg

1️⃣2️⃣ Оновлення інформації про задачу.  
PUT → `http://localhost:5000/api/tasks/<id>`  
Ендпоінт дозволяє оновити дані конкретної задачі за її ідентифікатором. Доступ мають лише авторизовані користувачі: https://prnt.sc/xcL5ZtTUwPV4

1️⃣3️⃣ Видалення задачі.  
DELETE → `http://localhost:5000/api/tasks/<id>`  
Ендпоінт дозволяє видалити конкретну задачу за її ідентифікатором. Видалення може виконати лише авторизований користувач: https://prnt.sc/slJar81oqq1d

1️⃣4️⃣ Після перевірки всіх ендпоінтів.  
Після успішної роботи з API можна перейти до веб-інтерфейсу, щоб переглянути задачі: https://prnt.sc/JLwBSfUSETjQ

### Моніторинг застосунку через Prometheus і Grafana
Після запуску **Prometheus** за адресою `http://localhost:9090/` можна перевірити, що всі налаштовані джерела метрик (jobs) підключені та успішно передають дані. 
Prometheus збирає метрики з бекенда (Flask), фронтенда (Node.js) та контейнерів (cAdvisor), що дозволяє відстежувати використання ресурсів і продуктивність сервісів.  
Для перевірки достатньо виконати кілька запитів у вкладці Graph:   

1️⃣ Cереднє використання CPU процесом за останню хвилину.  
   ```plaintext
   rate(process_cpu_seconds_total[1m])
   ```
Результат: https://prnt.sc/nogMhwNcQWnc  

2️⃣ Обсяг пам’яті, яку займає процес кожного контейнера.  
   ```plaintext
   process_resident_memory_bytes
   ```
Результат: https://prnt.sc/Kh6SpLpZ2RVz  

Після запуску **Grafana** за адресою `http://localhost:3033/` можна створювати власні дашборди або переглядати готові метрики.  
При першому вході використовується стандартна авторизація: Логін: `admin` та Пароль: `admin`  
У проєкті Grafana налаштована через механізм provisioning, який автоматично створює необхідні дашборди під час запуску контейнера.   
За замовчуванням додаються такі дашборди: https://prnt.sc/zvCb_14xZDkc  
1️⃣ flask_http_request_total.json — моніторинг HTTP-запитів у Flask: https://prnt.sc/FGOtWKrhFyze  
2️⃣ memory_monitoring.json — відстеження використання пам’яті процесів: https://prnt.sc/xtHrBcGZls0s  
3️⃣ nodejs_active_handles_total.json — активні дескриптори у Node.js: https://prnt.sc/a1WldjaRT2wx  
4️⃣ process_cpu_seconds_total.json — моніторинг навантаження на CPU: https://prnt.sc/OPpW0s5Jd8yV  

### Log Monitoring Stack (ELK + Filebeat)
Після запуску Kibana за адресою `http://localhost:5601/app/management/data/index_management/indices` перевіряється працездатність лог-стека, який включає сервіси Filebeat, Logstash, Elasticsearch та Kibana.  
Цей стек забезпечує централізований збір логів із Docker-контейнерів, їх обробку, індексацію та візуалізацію.  

1️⃣ Після успішного підключення всі зібрані логи потрапляють до Elasticsearch, де автоматично створюються індекси у форматі: docker-logs-YYYY.MM.DD https://prnt.sc/X1CkTKKaOfcg  
2️⃣ Перейди до розділу: `http://localhost:5601/app/discover` та натисни Create data view https://prnt.sc/czByLk6U-LDU  
3️⃣ У полі Index pattern введи: docker-logs-* і збережи: https://prnt.sc/bt5L0hVFAsYv  
4️⃣ Після створення data view відкриються всі зібрані логи в реальному часі — це підтверджує, що ланцюг Filebeat → Logstash → Elasticsearch → Kibana працює коректно.  https://prnt.sc/GqH6aAr0cgw6  
5️⃣ Наприклад, щоб переглянути всі 404 помилки з бекенду, достатньо задати фільтри: `container.name.keyword: backend-python` та `message : 404` https://prnt.sc/7nVvC7vB7RSq

## ✅ Тестування
### Юніт-тести бекенду
Для перевірки коректності роботи API створено юніт-тести у файлі: backend/tests/test_routes.py.  

Тести перевіряють основні маршрути Flask-застосунку:  
1️⃣ Реєстрацію користувача — POST /api/users/register  
2️⃣ Авторизацію користувача — POST /api/users/login  
3️⃣ Створення задачі — POST /api/tasks  
4️⃣ Отримання списку всіх задач — GET /api/tasks  
5️⃣ Публічний маршрут — GET /api/tasks/public  

Під час виконання тестів Flask-застосунок використовує SQLite in-memory базу дани.   
Це дозволяє проводити перевірки ізольовано, без впливу на реальні дані, а після кожного тесту база очищається автоматично.  

**Запуск тестів**  
Тести можна запустити кількома способами:  
🔹 Усередині контейнера:  
   ```plaintext
   python -m unittest tests/test_routes.py
   ```
Результат: https://prnt.sc/GOY7GW48-1dF

Або скорочено:  
```plaintext
   python -m unittest -b tests/test_routes.py
   ```
Результат: https://prnt.sc/94T_3aoWmk66  

🔹 Через Docker:    
   ```plaintext
   docker exec -it backend-python python -m unittest tests/test_routes.py
   ```
Результат: https://prnt.sc/lNNJgzvOUiV0  

> Підсумок: Юніт-тести підтверджують коректну роботу основних API-ендпоінтів.

### Локальні функціональні тести фронтенду (Jest + React Testing Library)
Для перевірки коректної роботи клієнтської частини застосунку реалізовано функціональне тестування React-компонентів.  
Тести знаходяться у файлі: frontend/src/App.test.js.  

Тести перевіряють:  
1️⃣ Відображення заголовку списку задач — TaskList heading  
2️⃣ Коректне відображення задач з API  

Тести використовують моки axios, тому вони ізольовані від реального бекенду та бази даних.  

**Запуск тестів**  
🔹 В контейнері фронтенду frontend-react або локально:  
   ```plaintext
   npm test
   ```
або  
   ```plaintext
   npx jest --runInBand
   ```
Результат: https://prnt.sc/VRSaJDM1d5ox

> Підсумок: Локальні функціональні тести фронтенду дозволяють перевірити коректне відображення компонентів React та взаємодію з API через моки, забезпечуючи ізольовану перевірку клієнтської частини застосунку без реального бекенду та бази даних.  

### Автоматичне інтеграційне тестування Selenium
Для перевірки взаємодії між фронтендом, бекендом і базою даних реалізовано інтеграційні тести за допомогою Selenium.  
Тести знаходяться у файлі: selenium/test_integration.py.  
Вони автоматично перевіряють роботу застосунку в реальному середовищі, включаючи:  
1️⃣ Роботу фронтенду (React)  
2️⃣ Коректне виконання запитів до бекенду (Flask API)  
3️⃣ Роботу бекенду з тестовою базою даних PostgreSQL  
Тобто ці тести покривають end-to-end сценарії для ключових користувацьких потоків.  

**Порядок виконання тестування**  
1️⃣ У файлі selenium/test_integration.py знайти "url ="  
Їх буде два:  
   - Розкоментованим залиште той, який відповідає системі, на якій запускається тестовий стек (Windows/Mac або Linux).  
   - Інший рядок повинен залишитися закоментованим.

2️⃣ З кореня проєкту запустити тестовий стек:  
   ```plaintext
   docker compose -f docker-compose.test.yml up -d --build
   ```
Після підняття контейнерів  (команда, що виконується всередині контейнера):  
3️⃣ Дочекатися, поки всі сервіси будуть у статусі healthy.  
Подивитись:  
   ```plaintext
   docker ps -а
   ```
Результат: https://prnt.sc/Wb7bkHLGcVfJ  
Після виконання тестів контейнер selenium-test зупиняється самостійно.  
4️⃣ Подивитись логи контейнера selenium-test. Інтеграційні тести автоматично виконуються у контейнері selenium-test і записують логи про виконання.  
Перевірити командою:  
   ```plaintext
   docker logs selenium-test
   ```
Результат: https://prnt.sc/nWjDaVlMqCSV
Контейнер запускає тести автоматично за допомогою команди:  `CMD ["python3", "test_integration.py"]`  
5️⃣ Завершення тестування. Для видалення всього тестового середовища та контейнерів:  
   ```plaintext
   docker compose -f docker-compose.test.yml down --rmi all -v
   ```
> Підсумок: інтеграційне тестування забезпечує перевірку взаємодії між фронтендом, бекендом і базою даних у реальному середовищі, дозволяючи виявити помилки у повних сценаріях використання застосунку.  
Selenium автоматизує сценарії користувача, що забезпечує достовірну оцінку працездатності всієї системи.

**Ручний запуск інтеграційних тестів Selenium**  
За замовчуванням контейнер selenium-test під час запуску автоматично виконує тести test_integration.py і після завершення зупиняється.  
Щоб виконувати тести вручну потрібно:  
1️⃣ В selenium/Dockerfile закоментувати рядок: `# CMD ["python3", "test_integration.py"]`  
2️⃣ З кореня проєкту запустити тестовий стек:  
   ```plaintext
   docker compose -f docker-compose.test.yml up -d --build
   ```
Контейнер запуститься, але не виконуватиме тести автоматично.  
3️⃣ Дочекатися, поки всі сервіси будуть у статусі healthy.  
Подивитись:  
   ```plaintext
   docker ps -а
   ```
Результат: https://prnt.sc/bpWv2wTxuVBr  
4️⃣ Зайти всередину контейнера:  
   ```plaintext
   docker exec -it selenium-test sh
   ```
і запустити команду  
   ```plaintext
   python3 test_integration.py
   ```
Результат: https://prnt.sc/WqMj_Sbc5kE0  
5️⃣ Завершення тестування. Для видалення всього тестового середовища та контейнерів:  
   ```plaintext
   docker compose -f docker-compose.test.yml down --rmi all -v
   ```
